<%= form_for(@combo) do |f| %>
  <% if @combo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@combo.errors.count, "error") %> prohibited this combo from being saved:</h2>

      <ul>
      <% @combo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fruit_id %><br>
    <%= f.text_field :fruit_id %>
  </div>
  <div class="field">
    <%= f.label :vegetable_id %><br>
    <%= f.text_field :vegetable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
